---
name: FrancescoFerri's Portfolio
markdown: kramdown
include: [_pages, _node_modules]

collections:
    projects:
        output: true

defaults:
    - scope:
          path: ""
          type: "projects"
      values:
          layout: "project"
